import java.util.Random;

public class TESTER {

	public static void main(String [] args){

		Random rand = new Random();
		int range = 12;
		HashTable table = new HashTable(range);
		
		table.add(new Tuple(rand.nextInt(range), "Yep"));
		
		table.printTable();
	}
}
