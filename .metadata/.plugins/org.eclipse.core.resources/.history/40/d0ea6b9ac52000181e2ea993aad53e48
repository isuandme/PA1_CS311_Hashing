import java.util.ArrayList;
import java.util.Random;

public class TESTER {

	public static void main(String [] args){

		Random rand = new Random();

		int  n = rand.nextInt(50) + 1;
		//50 is the maximum and the 1 is our minimum 
		
		HashFunction hash = new HashFunction(10);
		ArrayList<Tuple> arr = new ArrayList<Tuple>();
		arr.add(new Tuple(hash.hash(rand.nextInt(10)), "Yep"));
		arr.add(new Tuple(hash.hash(rand.nextInt(10)), "nope"));
		arr.add(new Tuple(hash.hash(rand.nextInt(10)), "Yep"));
		arr.add(new Tuple(hash.hash(rand.nextInt(10)), "Sure"));
		arr.add(new Tuple(hash.hash(rand.nextInt(10)), "Okay"));
		arr.add(new Tuple(hash.hash(rand.nextInt(10)), "Yass"));
		System.out.println();
	}
}
