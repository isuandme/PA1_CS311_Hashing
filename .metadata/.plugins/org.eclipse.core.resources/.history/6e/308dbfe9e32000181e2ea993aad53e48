import java.util.Random;

public class TESTER {

	private enum str {
		Yep, Nope, okay, maybe, whynot, sure, who, me, two, when, cry, naaa, girls, boys, face, space
	}
	
	
	public static void main(String [] args){

		Random rand = new Random();
		int range = 12;
		HashTable table = new HashTable(range);
		
		table.add(new Tuple(rand.nextInt(range), enum str[rand.nextInt(range)]));
		table.printTable();
	}
}
