import java.util.Random;

public class TESTER {

	private enum Str {
		Yep, Nope, okay, maybe, whynot, sure, who, me, two, when, cry, naaa, girls, boys, face, space
	}
	
	
	public static void main(String [] args){

		Random rand = new Random();
		int range = 12;
		HashTable table = new HashTable(range);
		for(int i = 0; i < 1000; i++){
			
			table.add(new Tuple(rand.nextInt(range), str[rand.nextInt(range)]%16));
		}
		
		table.printTable();
	}
}
