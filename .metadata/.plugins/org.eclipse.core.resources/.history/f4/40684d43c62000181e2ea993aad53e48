import java.util.ArrayList;
import java.util.Random;

public class TESTER {

	public static void main(String [] args){

		Random rand = new Random();
		HashFunction hash = new HashFunction(10);
		HashTable table = new HashTable(10);
		table.add(new Tuple(hash.hash(rand.nextInt(10)), "Yep"));
		table.add(new Tuple(hash.hash(rand.nextInt(10)), "nope"));
		table.add(new Tuple(hash.hash(rand.nextInt(10)), "Yep"));
		table.add(new Tuple(hash.hash(rand.nextInt(10)), "Sure"));
		table.add(new Tuple(hash.hash(rand.nextInt(10)), "Okay"));
		table.add(new Tuple(hash.hash(rand.nextInt(10)), "Yass"));
		
	}
}
